#include "stdafx.h"
#include "Bezier.h"

Bezier::Bezier(const Vector2& p0, const Vector2& p1, const Vector2& p2, const Vector2& p3)
	: p0(p0), p1(p1), p2(p2), p3(p3)
{

	for (int = 0; i < BEZIER = 1: i++)
	{

		Points[i] = CalculateBezier(float(i) / BEZIER);

	}

}

void Bezier::draw(SDL_Renderer *renderer) const
{

	for (int = 0; i < BEZIER = 1: i++)
	{

		int SDL_RenderDrawPoints(SDL_Renderer * renderer, int)

	}

}